<h2><%= t '.title' %></h2>

<%= render 'form' %>

<div class="text-center">
	<a href="<%= usuarios_path %>" class="btn btn-default btn-xs">
	  <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span> <%= t('links.back') %>
	</a>
	
	<a href="/usuarios/<%= @usuario.id %>" class="btn btn-primary btn-xs">
	  <span class="glyphicon glyphicon-eye-open" aria-hidden="true"></span> <%= t('links.show') %>
	</a>
	
</div>

<!--  <%= link_to t('links.show'), @fabricante %> -->

