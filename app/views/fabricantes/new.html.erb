<h1><%= t '.title' %></h1>

<%= render 'form' %>

<div class="text-center">
	<a href="<%= fabricantes_path %>" class="btn btn-default btn-xs">
	  <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span> <%= t('links.back') %>
	</a>
</div>